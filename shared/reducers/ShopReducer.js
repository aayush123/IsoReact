import Immutable from 'immutable';
import { browserHistory } from 'react-router';

export default function ShopReducer(state={'x':10}, action){
  switch (action.type) {
    default:
      return state;
  }
}
